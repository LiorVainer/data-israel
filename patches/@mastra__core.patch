diff --git a/dist/chunk-IYSW4VN7.js b/dist/chunk-IYSW4VN7.js
index d3fee532b2ac8bb2a7b5df456493a64508d4c71d..fbf5ebea74faa89cc230d949ef382558850b54ae 100644
--- a/dist/chunk-IYSW4VN7.js
+++ b/dist/chunk-IYSW4VN7.js
@@ -20822,12 +20822,12 @@ ${errorMessages}`,
         const agentInputSchema = z.object({
           prompt: z.string().describe("The prompt to send to the agent"),
           // Using .nullish() instead of .optional() because OpenAI sends null for unfilled optional fields
-          threadId: z.string().nullish().describe("Thread ID for conversation continuity for memory messages"),
-          resourceId: z.string().nullish().describe("Resource/user identifier for memory messages"),
-          instructions: z.string().nullish().describe(
+          threadId: z.string().optional().describe("Thread ID for conversation continuity for memory messages"),
+          resourceId: z.string().optional().describe("Resource/user identifier for memory messages"),
+          instructions: z.string().optional().describe(
             "Additional instructions to append to the agent instructions. Only provide if you have specific guidance beyond what the agent already knows. Leave empty in most cases."
           ),
-          maxSteps: z.number().min(3).nullish().describe("Maximum number of execution steps for the sub-agent")
+          maxSteps: z.number().min(3).optional().describe("Maximum number of execution steps for the sub-agent")
           // using minimum of 3 to ensure if the agent has a tool call, the llm gets executed again after the tool call step, using the tool call result
           // to return a proper llm response
         });
diff --git a/dist/chunk-QLC3UBDY.cjs b/dist/chunk-QLC3UBDY.cjs
index cabe99810b79295e38c6f58a603c0bd41cbc9eb6..ec9fe81b8832c7284425ef7f7a3fcbbe1a0dbdd7 100644
--- a/dist/chunk-QLC3UBDY.cjs
+++ b/dist/chunk-QLC3UBDY.cjs
@@ -20847,12 +20847,12 @@ ${errorMessages}`,
         const agentInputSchema = v4.z.object({
           prompt: v4.z.string().describe("The prompt to send to the agent"),
           // Using .nullish() instead of .optional() because OpenAI sends null for unfilled optional fields
-          threadId: v4.z.string().nullish().describe("Thread ID for conversation continuity for memory messages"),
-          resourceId: v4.z.string().nullish().describe("Resource/user identifier for memory messages"),
-          instructions: v4.z.string().nullish().describe(
+          threadId: v4.z.string().optional().describe("Thread ID for conversation continuity for memory messages"),
+          resourceId: v4.z.string().optional().describe("Resource/user identifier for memory messages"),
+          instructions: v4.z.string().optional().describe(
             "Additional instructions to append to the agent instructions. Only provide if you have specific guidance beyond what the agent already knows. Leave empty in most cases."
           ),
-          maxSteps: v4.z.number().min(3).nullish().describe("Maximum number of execution steps for the sub-agent")
+          maxSteps: v4.z.number().min(3).optional().describe("Maximum number of execution steps for the sub-agent")
           // using minimum of 3 to ensure if the agent has a tool call, the llm gets executed again after the tool call step, using the tool call result
           // to return a proper llm response
         });
diff --git a/dist/chunk-FWDG2WUV.js b/dist/chunk-FWDG2WUV.js
--- a/dist/chunk-FWDG2WUV.js
+++ b/dist/chunk-FWDG2WUV.js
@@ -30,8 +30,8 @@
       }
       if (isZodObject(schema)) {
         this.originalTool.inputSchema = schema.extend({
-          suspendedToolRunId: z.string().describe("The runId of the suspended tool").nullable().optional(),
-          resumeData: z.any().describe("The resumeData object created from the resumeSchema of suspended tool").optional()
+          suspendedToolRunId: z.string().describe("The runId of the suspended tool").optional(),
+          resumeData: z.string().describe("The resumeData object created from the resumeSchema of suspended tool (JSON-encoded)").optional()
         });
       } else {
         const jsonSchema2 = standardSchemaToJSONSchema(schema, { io: "input" });
@@ -39,11 +39,12 @@
           jsonSchema2.properties = {
             ...jsonSchema2.properties,
             suspendedToolRunId: {
-              type: ["string", "null"],
+              type: "string",
               description: "The runId of the suspended tool"
             },
             resumeData: {
-              description: "The resumeData object created from the resumeSchema of suspended tool"
+              type: "string",
+              description: "The resumeData object created from the resumeSchema of suspended tool (JSON-encoded)"
             }
           };
           this.originalTool.inputSchema = toStandardSchema(jsonSchema2);
diff --git a/dist/chunk-R2V6BCYX.cjs b/dist/chunk-R2V6BCYX.cjs
--- a/dist/chunk-R2V6BCYX.cjs
+++ b/dist/chunk-R2V6BCYX.cjs
@@ -32,8 +32,8 @@
       }
       if (chunkRLPRSIR2_cjs.isZodObject(schema)) {
         this.originalTool.inputSchema = schema.extend({
-          suspendedToolRunId: v4.z.string().describe("The runId of the suspended tool").nullable().optional(),
-          resumeData: v4.z.any().describe("The resumeData object created from the resumeSchema of suspended tool").optional()
+          suspendedToolRunId: v4.z.string().describe("The runId of the suspended tool").optional(),
+          resumeData: v4.z.string().describe("The resumeData object created from the resumeSchema of suspended tool (JSON-encoded)").optional()
         });
       } else {
         const jsonSchema2 = chunkXB4FLS7A_cjs.standardSchemaToJSONSchema(schema, { io: "input" });
@@ -41,11 +41,12 @@
           jsonSchema2.properties = {
             ...jsonSchema2.properties,
             suspendedToolRunId: {
-              type: ["string", "null"],
+              type: "string",
               description: "The runId of the suspended tool"
             },
             resumeData: {
-              description: "The resumeData object created from the resumeSchema of suspended tool"
+              type: "string",
+              description: "The resumeData object created from the resumeSchema of suspended tool (JSON-encoded)"
             }
           };
           this.originalTool.inputSchema = chunkXB4FLS7A_cjs.toStandardSchema(jsonSchema2);
