# ===========================================
# Clerk Authentication Configuration
# ===========================================

# Clerk API Keys (from Clerk Dashboard > API Keys)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Clerk Frontend API URL (from Clerk Dashboard > JWT Templates > Convex)
# This is the Issuer URL used for Convex JWT validation
# Development format: https://verb-noun-00.clerk.accounts.dev
# Production format: https://clerk.<your-domain>.com
CLERK_FRONTEND_API_URL=https://your-instance.clerk.accounts.dev

# Clerk Auth URLs (optional - defaults work fine)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# ===========================================
# Google OAuth Configuration (Custom Credentials)
# ===========================================
#
# These are NOT environment variables - configure in Clerk Dashboard!
#
# Steps to set up custom Google OAuth:
#
# 1. Go to Google Cloud Console (https://console.cloud.google.com)
# 2. Create a new project or select existing one
# 3. Navigate to APIs & Services > Credentials
# 4. Click "Create Credentials" > "OAuth client ID"
# 5. Select "Web application" as application type
# 6. Add authorized JavaScript origins:
#    - http://localhost:3000 (development)
#    - https://your-domain.com (production)
# 7. Add authorized redirect URIs:
#    - Get this from Clerk Dashboard (see step 10)
# 8. Click "Create" and copy Client ID and Client Secret
#
# 9. Go to Clerk Dashboard (https://dashboard.clerk.com)
# 10. Navigate to User & Authentication > Social Connections
# 11. Click on Google
# 12. Toggle "Use custom credentials" ON
# 13. Enter your Google Client ID and Client Secret
# 14. Copy the "Authorized Redirect URI" shown in Clerk
# 15. Go back to Google Cloud Console and add this URI
#
# Your Google OAuth credentials:
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-client-secret
# (These go in Clerk Dashboard, NOT in .env)

# ===========================================
# Convex Configuration
# ===========================================

# Convex Deployment URL (from Convex Dashboard)
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Convex Admin Key (for server-side operations)
# Get from Convex Dashboard > Settings > Deploy Key
CONVEX_ADMIN_KEY=prod:your-admin-key
